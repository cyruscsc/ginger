// import { User } from './models/User';

// const user = new User({ name: 'new user', age: 40 });

// user.events.on('change', () => {
//   console.log('changed!');
// });

// user.events.trigger('change');
