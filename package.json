{
  "dependencies": {
    "axios": "^1.5.1",
    "concurrently": "^8.2.1",
    "parcel": "^2.10.0"
  },
  "scripts": {
    "start": "concurrently npm:start:*",
    "start:db": "node_modules/.bin/json-server -w db.json",
    "start:parcel": "npx parcel index.html"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "json-server": "^0.17.4"
  }
}
